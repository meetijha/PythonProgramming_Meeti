
#always default value has to be the last parameter
def sum(a,b=1):
    return a+b

print(sum(5))

# def sum2(a=1,b): this gives compile time error
#as default parameter has to be at end not beginning

